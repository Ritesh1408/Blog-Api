<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ThinkFlare</title>
    <link rel="icon" href="../favicon.ico" type="image/x-icon">
</head>
<body>
    <div class="top-bar">
        <div id="logo">
            <a href="/">ThinkFlare 🔥</a>
        </div>
        <div class="tabs">
            <div class="tab"><a href="/">Home</a></div>
            <% if(isAuthenticated) { %>
                <div class="tab"><a href="/myblogs">My Blogs</a></div>
            <% } %>
        </div>
        <div class="logout">
            <% if(isAuthenticated) { %>
                <a id="logout-btn" href="/logout">LogOut</a>
            <% } else { %>
                <a id="logout-btn" href="/login">Login</a>
            <% } %>
        </div>
    </div>
</body>
</html>